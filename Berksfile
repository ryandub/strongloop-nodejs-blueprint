#!/usr/bin/env ruby
#^syntax detection

site :opscode

cookbook 'apt',
  :git => 'https://github.rackspace.com/Cookbooks/apt.git'

cookbook 'firewall',
  :git => 'https://github.com/opscode-cookbooks/firewall.git'

cookbook 'nginx'

cookbook 'strongloop',
  :git => 'https://github.com/ryandub/strongloop-node.git'
